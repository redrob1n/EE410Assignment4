//returning theta 
float runAway(float feelForce)
{


    if(false) //dummy for compile
    {
        return vectorAngle;
    }
    if(false) //dummy for compile
    {
        return vectorAngle;
    }

}